expr: Test ResNet (MNIST)

device: &device cuda:0
parallel: false

data:
  dataset: MNIST
  root: data/
  download: True
  mode: test

model:
  name: ResNet

hyperparameters:
  batch_size: 32

save:
  weights_path: saved/weights
  weights_filename: resnet_mnist.epochs_020.pth